id: ui.back_to_list_button
name: Back To List Button
type: ui
description: Button that exits the editor and returns to the records list.
files:
  - src/features/records/editor/actions.tsx
functions:
  - handleBackToList
owned_by_flows:
  - flow.text_record_lifecycle
risk_level: low
change_rules:
  - Keep back-navigation aligned with the list route.
failure_modes:
  - Button sends users to an outdated route.
