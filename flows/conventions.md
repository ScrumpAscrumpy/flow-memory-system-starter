# Flow Memory Conventions

Use this file to record project-specific naming rules, exceptions, or workflow notes that differ from the default Flow Memory System conventions.

## Current Guidance

- Store one Flow Card per file in `flows/cards/`.
- Store one Node definition per file in `flows/nodes/`.
- Keep `anchor_files` focused on the flow's core logic.
- Add project-specific exceptions here as the system evolves.
